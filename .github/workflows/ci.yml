name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

# Opt into Node.js 24 for JavaScript actions now so we don't inherit the
# default flip scheduled for 2026-06-02. Remove once all pinned actions
# natively ship with a Node 24 runtime.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  # SwiftShader software Vulkan ICD packaged by Silk.NET (Apache-2.0)
  # — used only on the Windows leg. Linux uses NcStudios/VulkanCI@vnext
  # (see below) because Silk.NET's Linux SwiftShader build SIGSEGV'd
  # mid-suite across multiple test fixtures, while NcStudios ships a
  # SwiftShader binary built against ubuntu-20.04 glibc plus a custom
  # Vulkan loader with LOADER_USE_UNSAFE_FILE_SEARCH=ON that was the
  # only software-Vulkan-on-Linux recipe in the ecosystem we found
  # surviving a full xUnit sweep.
  SILK_VULKAN_SWIFTSHADER_VERSION: "2025.9.8"
  # Khronos Vulkan loader (vulkan-1.dll) packaged by Silk.NET, Windows
  # only. Hosted Windows runners don't ship a Vulkan loader OOTB, so we
  # provision it alongside the ICD; VK_DRIVER_FILES then routes the
  # loader's driver lookup exclusively at SwiftShader.
  SILK_VULKAN_LOADER_VERSION: "2025.9.12"

jobs:
  build-test:
    name: Build & Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            rid: win-x64
          - os: ubuntu-latest
            rid: linux-x64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Both runners provision SwiftShader (software Vulkan ICD) so the
      # driver-gated test subset executes rather than skipping. Tests
      # that depend on hardware-driver semantics still short-circuit via
      # VulkanDriverProbe.IsSoftwareDriver — loosening those gates is a
      # follow-up once we know SwiftShader's failure modes on CI.

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: Directory.Packages.props

      # Cache the downloaded Vulkan-Headers archive so subsequent runs
      # don't hit GitHub. Invalidates when Directory.Build.props changes —
      # where VulkanHeadersVersion lives.
      - name: Cache Vulkan-Headers
        uses: actions/cache@v4
        with:
          path: native/downloaded
          key: vulkan-headers-${{ matrix.os }}-${{ hashFiles('Directory.Build.props') }}

      # ── Vulkan loader + SwiftShader ICD ──────────────────────────────
      # Two provisioning paths because the only software-Vulkan recipe we
      # found that survives a full xUnit sweep on Linux is NcStudios's
      # purpose-built artifact set:
      #   - Windows: Silk.NET loader + Silk.NET SwiftShader nupkgs. Cheap
      #     downloads, fast cache, already proven stable on this leg.
      #   - Linux: NcStudios/VulkanCI@vnext, which installs their custom
      #     Vulkan SDK (Khronos loader built with
      #     LOADER_USE_UNSAFE_FILE_SEARCH=ON) and a SwiftShader binary
      #     built against ubuntu-20.04 glibc. The action also generates
      #     a fresh ICD JSON manifest with an absolute library_path and
      #     sets only VK_DRIVER_FILES (we used to set VK_ICD_FILENAMES
      #     too — possibly double-loading). Other ecosystem actions
      #     (jakoch/install-vulkan-sdk-action) explicitly refuse to ship
      #     SwiftShader for Linux, so this recipe is a load-bearing
      #     reference.

      # Windows: pinned Silk.NET nupkgs. Cache hit skips two ~15 MB
      # downloads. Cache key invalidates whenever either pinned version
      # env var changes.
      - name: Cache Vulkan ICD bundle
        if: matrix.os == 'windows-latest'
        id: cache-vk
        uses: actions/cache@v4
        with:
          path: build/vulkan/${{ matrix.rid }}
          key: vk-${{ matrix.rid }}-loader${{ env.SILK_VULKAN_LOADER_VERSION }}-sws${{ env.SILK_VULKAN_SWIFTSHADER_VERSION }}

      - name: Provision Khronos loader + SwiftShader ICD (Windows)
        if: matrix.os == 'windows-latest' && steps.cache-vk.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          $rid = "${{ matrix.rid }}"
          $dst = "build/vulkan/$rid"
          New-Item -ItemType Directory -Force -Path $dst | Out-Null
          # .nupkg files are zips; rename to .zip so Expand-Archive
          # accepts them without extension-sniffing complaints.
          $loaderZip = Join-Path $env:RUNNER_TEMP "vk-loader.zip"
          $swsZip    = Join-Path $env:RUNNER_TEMP "vk-sws.zip"
          $loaderUnp = Join-Path $env:RUNNER_TEMP "vk-loader"
          $swsUnp    = Join-Path $env:RUNNER_TEMP "vk-sws"
          Invoke-WebRequest -UseBasicParsing `
            -Uri "https://www.nuget.org/api/v2/package/Silk.NET.Vulkan.Loader.Native/${{ env.SILK_VULKAN_LOADER_VERSION }}" `
            -OutFile $loaderZip
          Invoke-WebRequest -UseBasicParsing `
            -Uri "https://www.nuget.org/api/v2/package/Silk.NET.Vulkan.SwiftShader.Native/${{ env.SILK_VULKAN_SWIFTSHADER_VERSION }}" `
            -OutFile $swsZip
          if (Test-Path $loaderUnp) { Remove-Item -Recurse -Force $loaderUnp }
          if (Test-Path $swsUnp)    { Remove-Item -Recurse -Force $swsUnp }
          Expand-Archive -Path $loaderZip -DestinationPath $loaderUnp -Force
          Expand-Archive -Path $swsZip    -DestinationPath $swsUnp    -Force
          Copy-Item -Path "$loaderUnp/runtimes/$rid/native/*" -Destination $dst -Force
          Copy-Item -Path "$swsUnp/runtimes/$rid/native/*"    -Destination $dst -Force
          Get-ChildItem $dst | ForEach-Object { Write-Host ("  {0,12}  {1}" -f $_.Length, $_.Name) }

      - name: Configure Vulkan environment (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $dir = Join-Path $env:GITHUB_WORKSPACE "build/vulkan/${{ matrix.rid }}"
          $icd = Join-Path $dir "vk_swiftshader_icd.json"
          if (-not (Test-Path $icd)) { throw "SwiftShader ICD manifest missing: $icd" }
          # VK_DRIVER_FILES (loader 1.3.207+) overrides system ICD
          # discovery and routes exclusively at SwiftShader. Only set
          # VK_DRIVER_FILES — setting VK_ICD_FILENAMES (the pre-1.3.207
          # spelling) alongside caused double-load symptoms on Linux
          # during the issue-32 investigation; keep it off here too.
          "VK_DRIVER_FILES=$icd" >> $env:GITHUB_ENV
          # Prepend so LoadLibrary("vulkan-1.dll") finds the Silk.NET
          # loader before anything system-wide.
          "PATH=$dir;$env:PATH" >> $env:GITHUB_ENV

      # Linux: NcStudios/VulkanCI installs their Vulkan SDK + SwiftShader
      # artifacts and configures VK_DRIVER_FILES, LD_LIBRARY_PATH,
      # VK_LAYER_PATH for us. Pinned to v1.3, the latest tagged release
      # (the vnext branch has no attached release binaries).
      - name: Provision Vulkan + SwiftShader (Linux)
        if: matrix.os == 'ubuntu-latest'
        uses: NcStudios/VulkanCI@v1.3

      # ── /Vulkan loader + SwiftShader ICD ─────────────────────────────

      - name: Restore dotnet tools
        run: dotnet tool restore

      - name: Build (Release)
        run: dotnet build Ahjo.Vulkan.slnx -c Release --nologo

      - name: Test — Ahjo.Vulkan.Native.Tests
        run: dotnet test tests/Ahjo.Vulkan.Native.Tests/Ahjo.Vulkan.Native.Tests.csproj -c Release --no-build --nologo -l "console;verbosity=detailed"

      # Detailed console logging prints each test name as it starts so a
      # native crash (SIGSEGV/AV) names the offending test in the log
      # instead of just surfacing as exit 139 with no context. --blame
      # also writes Sequence.xml naming the test that was running when
      # the test host crashed — printed below on step failure.
      - name: Test — Ahjo.Vulkan.Tests
        run: dotnet test tests/Ahjo.Vulkan.Tests/Ahjo.Vulkan.Tests.csproj -c Release --no-build --nologo -l "console;verbosity=detailed" --blame --blame-crash --results-directory TestResults

      - name: Print blame logs on failure
        if: failure()
        shell: bash
        run: |
          echo "::group::Blame Sequence files"
          find TestResults -type f \( -name 'Sequence_*.xml' -o -name '*.txt' -o -name '*.dmp.txt' \) -print -exec cat {} \; 2>/dev/null || true
          echo "::endgroup::"

      # Issue 28 — Native AOT smoke. Publishing under PublishAot=true
      # is the load-bearing check: trim warnings + ILC errors fail the
      # build, so any reflection / dynamic-codegen creep in the wrapper
      # is caught here. With SwiftShader provisioned the published exe
      # also runs the full render→PNG round-trip, exercising the whole
      # wrapper surface under AOT codegen.
      - name: Setup MSVC dev environment
        if: matrix.os == 'windows-latest'
        uses: ilammy/msvc-dev-cmd@v1

      - name: AOT publish — AotSmoke
        if: matrix.os == 'windows-latest'
        run: dotnet publish samples/AotSmoke/AotSmoke.csproj -c Release -r win-x64 --nologo -p:IlcUseEnvironmentalTools=true

      - name: AOT smoke run
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $exe = "samples/AotSmoke/bin/x64/Release/net10.0/win-x64/publish/AotSmoke.exe"
          if (-not (Test-Path $exe)) { throw "AOT smoke binary missing: $exe" }
          & $exe
          if ($LASTEXITCODE -ne 0) { throw "AOT smoke exited with code $LASTEXITCODE" }
